export class Artist {
  constructor(public name: string, public location: string, public genre: string, public members: string[], public bio: string, public lookingFor: string[], public available: string) {}
}
