import { LookingForPipe } from './looking-for.pipe';

describe('LookingForPipe', () => {
  it('create an instance', () => {
    const pipe = new LookingForPipe();
    expect(pipe).toBeTruthy();
  });
});
