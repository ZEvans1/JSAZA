import { GenreGroupPipe } from './genre-group.pipe';

describe('GenreGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new GenreGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
