import { AvailableGroupPipe } from './available-group.pipe';

describe('AvailableGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailableGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
